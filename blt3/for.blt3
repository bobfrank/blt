use()
    for(a, A)
        print(a)

for($el, S) @block
    while( !end(S) )
        el = next(S)
        @block($el = el, break=$["jmp for_end_" $0]) // would mean add the name used and represented by $el into the scope of @block with value el
    $["for_end_" $0 ":"]
